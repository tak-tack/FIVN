<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- <mapper namespace="egovframework.sqlmap.ausil.mappers.BoardMapper"> -->
<mapper namespace="com.sqlmap.study.mappers.BoardMapper">

	<resultMap id="Customer" type="com.epps.study.customer.vo.CustomerVO">
		<result property="customerCar_type_number" column="CAR_TYPE_NUMBER"/>
		<result property="customerCar_number" column="CAR_NUMBER"/>
		<result property="customerCustomer_name" column="CUSTOMER_NAME"/>
		<result property="customerCar_characteristic" column="CAR_CHARACTERISTIC"/>
		<result property="customerVisit_day" column="VISIT_DAY"/>
		<result property="customerPhone_number_1" column="PHONE_NUMBER_1"/>
		<result property="customerPhone_number_2" column="PHONE_NUMBER_2"/>
		<result property="customerCar_information_number" column="CAR_INFORMATION_NUMBER"/>
		<result property="customerColumn_1" column="COLUMN_1"/>
		<result property="customerNote_2018" column="NOTE_2018"/>
		<result property="customerNote_2019" column="NOTE_2019"/>
		<result property="customerNote_2020" column="NOTE_2020"/>
	</resultMap>
	
	
	<!-- 
	<select id="selectWordList"  resultMap="word">
		select
			단어번호,
			단어명,
			영문명,
			영문약어,
			단어정의
		from
			단어사전
		order by
			단어번호
		LIMIT #{recordCountPerPage} OFFSET #{firstRecordIndex}
		
	</select>
	
	<select id="selectWordListCnt"  resultType="java.lang.Integer">
		select
			COUNT(단어명)
		from
			단어사전	
	</select>
	
	<select id="selectWord"  resultMap="word">	
		select
			단어번호,
			단어명,
			영문명,
			영문약어,
			단어정의
		from
			단어사전
		where
			단어번호 = #{wordNum}	
	</select>
	
	<select id="insertWord"  resultMap="word">	
		INSERT INTO 
		단어사전 (
			단어번호,
			단어명,
			영문명,
			영문약어,
			단어정의
			)
	    VALUES(
		    #{wordNum}, 
		    #{wordNm}, 
		    #{wordEngNm}, 
		    #{engAbbNm}, 
		    #{wordJst}
		    )
	</select>
	
	<select id="deleteWord"  resultMap="word">	
		DELETE 
		FROM 
			단어사전
		WHERE 
			단어번호 = #{wordNum}
	</select>
	
	<select id="updateWord"  resultMap="word">	
		UPDATE 
			단어사전
		SET 
			단어명 = #{wordNm}, 
			영문명 = #{wordEngNm}, 
			영문약어 = #{engAbbNm}, 
			단어정의 = #{wordJst}
		WHERE 
			단어번호 = #{wordNum};

	</select>
	
 -->
</mapper>